/**
 * Buffer:Representação de um espaço na memória do computador para transitar dados de 
 * uma forma muito rápida. Escrever e ler da memória de forma binário ( Altamente performático).
 */

const buf = Buffer.from("ok")

console.log(buf.toJSON())